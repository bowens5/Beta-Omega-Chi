{
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "homepage": "https://bowens5.github.io/Beta-Omega-Chi",
  "scripts": {
    "predeploy": "npm --prefix react-app run build && rm -rf docs && cp -r react-app/build docs",
    "deploy": "gh-pages -d docs"
  }
}
